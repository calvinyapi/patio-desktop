[project]
name = "inference-model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "calvinyapi", email = "yapicalvinludo@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "opencv-python>=5.0.0.93",
]

[project.scripts]
inference-model = "inference_model.main:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"
